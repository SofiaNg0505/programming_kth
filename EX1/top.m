function element = top(stack)

% Return the copy of the top element of a stack. stack is a cell array of 
% Matlab strings and element is a Matlab string.
% YOUR NAME/NAMES, MONTH YEAR

element = stack(1,end);
end


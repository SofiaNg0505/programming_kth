function stack = push(element, stack);
% Add an element on the top of a stack, and return the new stack. stack 
% is a cell array of Matlab strings and element is a Matlab string.
% YOUR NAME/NAMES, MONTH YEAR
stack = [stack,element];
end
